export function DownloadButton({classes}) {
    return (
        <button className={`flex items-center justify-center ${classes} text-white rounded-lg py-3 px-6 shadow-md group ease-in-out transition-all hover:scale-105`}>
            MP4 - 1080p
        </button>
    )
}
